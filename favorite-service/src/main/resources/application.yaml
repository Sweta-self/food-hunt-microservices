server:
  port: 8084

spring:
  application:
    name: favorite-service

  datasource:
    url: jdbc:mysql://localhost:3306/foodhunt_favorite_service
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resource server:
        jwt:
          issuer-uri: https://dev-acun87tlm2zqnkn3.us.auth0.com/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka